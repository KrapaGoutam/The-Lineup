.next
coverage
node_modules
playwright-report
test-results
package-lock.json
supabase/.branches
supabase/.temp
# The user's own browser-tested design reference for Feature 021 (see
# docs/features/021-design-system-and-shell-refresh.md) -- a raw HTML/CSS
# scratch artifact meant for quick side-by-side visual scanning, not
# checked-in source; reformatting it isn't ours to do and could make it
# harder to compare frames at a glance.
design-system-reference.html

# The user's own untouched planning notes -- not part of any feature's
# diff, same reasoning as design-system-reference.html above. (STATUS.md
# and tasks/current-task.md were here too as of Feature 021, but Feature
# 023 onward actively maintains both as part of the documented workflow,
# so they're formatted like every other checked-in doc now.)
tasks/backlog.md
